<!-- res/drawable/rounded_textview.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/green" /> <!-- background color -->
    <corners android:radius="12dp" /> <!-- roundness -->
    <stroke
        android:width="1dp"
        android:color="@color/medGray" /> <!-- optional border -->
</shape>
